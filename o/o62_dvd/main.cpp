/*
	* AUTHOR : lo0ker
	* Problem : DVD
	* source : 
*/

#include <bits/stdc++.h>

using namespace std;

using ll = long long;
using vi = vector<int>;
using pii = pair<int, int>;
#define pb push_back
#define all(x) begin(x), end(x)
#define sz(x) (int) (x).size()

const int MOD = 1e9+7;

// segment MAX and MIN, then compare with a, b.

int main(){
  ios_base::sync_with_stdio(false);
	cin.tie(NULL);

  int n, k;
  cin >> n >> k;

	return 0;
}